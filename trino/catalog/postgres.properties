# PostgreSQL Catalog Configuration (Federated Queries)
connector.name=postgresql

# Connection settings
connection-url=jdbc:postgresql://postgres:5432/conveyor
connection-user=${ENV:POSTGRES_USER}
connection-password=${ENV:POSTGRES_PASSWORD}
