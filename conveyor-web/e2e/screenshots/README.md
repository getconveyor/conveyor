# E2E Test Screenshots

This directory stores screenshots captured during E2E test runs.

Screenshots are automatically captured:

- On test failures (for debugging)
- When explicitly called in tests

Files are named with timestamps to avoid conflicts.

## Cleanup

Run `rm -rf e2e/screenshots/*.png` to clean up old screenshots.
